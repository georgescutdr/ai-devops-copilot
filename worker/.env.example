# RabbitMQ connection
RABBITMQ_URL=amqp://rabbitmq:5672

# RabbitMQ queue name the worker listens to
QUEUE_NAME=tasks.queue

# Optional: chunk size for PDF/Doc processing if you extend tasks
CHUNK_SIZE=1000

# Optional: path to store temporary uploads if needed
UPLOAD_DIR=./uploads